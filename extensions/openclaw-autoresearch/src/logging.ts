/**
 * PR 2 skeleton only.
 * This module will own append-only JSONL logging and config-header handling in PRs 4 and 6.
 */
export function describeLoggingSkeleton(): string {
  return "jsonl logging not implemented yet";
}
