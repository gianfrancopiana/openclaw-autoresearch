/**
 * PR 2 skeleton only.
 * This module will own experiment command execution for run_experiment in PR 5.
 */
export function describeExecutionSkeleton(): string {
  return "experiment execution not implemented yet";
}
